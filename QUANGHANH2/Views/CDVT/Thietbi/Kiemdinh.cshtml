
@{
    ViewBag.Title = "Kiemdinh";
    Layout = "~/Views/Shared/_Layout_CDVT.cshtml";
}
<link href="~/dist/css/style.css" rel="stylesheet">
<!-- This page CSS -->
<link href="~/dist/css/pages/data-table.css" rel="stylesheet">

<link href="~/assets/Custom css/CDVT/TABLE_BORDERED.css" rel="stylesheet" />
<link href="~/lib/datepicker_src/dist/css/datepicker.min.css" rel="stylesheet" type="text/css">
<link href="~/lib/datepicker_src/css/style.css" rel="stylesheet " type="text/css ">
<link href="~/lib/datepicker_src/dist/css/datepicker.min.css " rel="stylesheet " type="text/css ">
<link href="~/lib/datepicker_src/dist/css/custom.css " rel="stylesheet " type="text/css ">
<link href="~/lib/datepicker_src/dist/css/font.css" rel="stylesheet" type="text/css">
<script src="~/lib/datepicker_src/dist/js/datepicker.js"></script>
<script src="~/lib/datepicker_src/dist/js/i18n/datepicker.vi.js"></script>

<div class="card">
    <div class="card-content">
        <div class="row">
            <h3 class="center"><b>THÔNG TIN KIỂM ĐỊNH</b></h3>
            <hr>
            <form action="" method="POST" id="mysearchform" role="form">
                <div class="row center">
                    <div class="col l4 m4 s12">
                        <div class="row">
                            <div class="col l10 m10 s12 input-field">
                                <input type="text" placeholder="Tìm kiếm theo mã thiết bị" class='form-control' id="equipmentId-search" />
                            </div>
                            <div class="col l10 m10 s12 input-field">
                                <input type="text" placeholder="Tìm kiếm theo tên thiết bị" class='form-control' id="equipmentName-search" />
                            </div>
                        </div>
                    </div>
                    <div class="col l4 m4 s12">
                        <div class="row">
                            <div class="col l10 m10 s12 offset-l1 offset-m1 input-field">
                                <label class="label">Ngày kiểm định dự tính từ</label>
                                <input required placeholder="thời gian bắt đầu kiểm định từ" type='text' class='datepicker-here form-control maxDate' data-language='vi' id="date-start-search" />
                            </div>
                            <div class="col l10 m10 s12 offset-l1 offset-m1 input-field">
                                <label class="label">Ngày kiểm định dự tính đến</label>
                                <input required placeholder="thời gian bắt đầu kiểm định đến" type='text' class='datepicker-here form-control minDate' data-language='vi' id="date-end-search" />
                            </div>
                        </div>
                    </div>
                    <div class="col l4 m4 s12">
                        <div class="row">
                            <div class="col l12 m12 s12 input-field center">
                                <a class="waves-effect waves-light btn blue darken-2 modal-close modal-trigger" id="searchButton1">Tìm kiếm</a>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
            <div class="col s12 table_container">
                <table class="table-bordered striped table-responsive centered"
                       style="margin-top:30px; text-align:center;" id="complex_header">
                    <thead>
                        <tr>
                            <th>Mã thiết bị</th>
                            <th>Tên thiết bị</th>
                            <th>Tình trạng hiện tại</th>
                            <th>Thời gian kiểm định dự tính</th>
                            <th>Thời gian bắt đầu kiểm định</th>
                            <th></th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>

<form action="" method="POST" id="myupdateform" role="form">
    <div id="myupdate" class="modal">
        <div class="modal-content">
            <div class="modal-header center">
                <h3>Bắt đầu kiểm định thiết bị</h3>
            </div>
            <div class="modal-body">
                <h5>Thiết bị sẽ được mang đi kiểm định trong hôm nay</h5>
            </div>
            <div class="modal-footer">
                <input type="hidden" readonly id="id-update" />
                <button class="waves-effect waves-light btn blue darken-2 modal-trigger">Lưu</button>
                <button type="button" class="btn light-blue modal-close darken-2" id="exit-update-button">Đóng</button>
            </div>
        </div>
    </div>
</form>

<form action="" method="POST" id="myeditform" role="form">
    <div id="myedit" class="modal">
        <div class="modal-content">
            <div class="modal-header center">
                <h3><b>Cập nhật kiểm định</b></h3>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col s12 m6">
                        <div class="form-group">
                            <label for="date">Thời gian kiểm định dự tính lần sau: </label>
                            <input required type='text' class='datepicker-here form-control minDate' data-language='vi' value="" id="date-temp-edit" />
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <input type="hidden" readonly id="id-edit" />
                <button class="waves-effect waves-light btn blue darken-2 modal-trigger">Lưu</button>
                <button type="button" class="btn light-blue modal-close darken-2" id="exit-edit-button">Đóng</button>
            </div>
        </div>
    </div>
</form>

<!-- ============================================================== -->
<!-- Apps -->
<!-- ============================================================== -->
<script src="~/dist/js/app.js"></script>
<script src="~/dist/js/app.init.js"></script>
<script src="~/dist/js/app-style-switcher.js"></script>
<!-- ============================================================== -->
<!-- This page plugin js -->
<!-- ============================================================== -->
<link href="~/css/transfer_mobilize_history_timeline.css" rel="stylesheet" />
<script src="~/js/Briefs_List.js"></script>
<link href="~/assets/Custom css/form-input-border.css" rel="stylesheet" />
<script>
    $(document).ready(function () {
        $('#date-temp-edit').datepicker({
            language: 'vi',
            minDate: new Date()
        })

        $('.datepicker-inline').remove();
        var today = new Date();
        var dd = today.getDate();
        var mm = today.getMonth() + 1; //January is 0!

        var yyyy = today.getFullYear();
        if (dd < 10) {
            dd = '0' + dd;
        }
        if (mm < 10) {
            mm = '0' + mm;
        }
        var today = dd + '/' + mm + '/' + (yyyy + 30);
        $(".minDate").each(function () {
            $(this).val(today);
        });
        today = new Date("1/1/1900");
        var dd = today.getDate();
        var mm = today.getMonth() + 1; //January is 0!

        var yyyy = today.getFullYear();
        if (dd < 10) {
            dd = '0' + dd;
        }
        if (mm < 10) {
            mm = '0' + mm;
        }
        var today = dd + '/' + mm + '/' + yyyy;
        $(".maxDate").val(today)
    });

    $(document).ready(function () {
        $('input[required]').each(function myfunction() {
            $(this).attr("oninvalid", "setCustomValidity('Không được bỏ trống ô này')");
            $(this).attr("oninput", "this.setCustomValidity('')");
        })
    })

    $('#checkbox-update').on('click', function (e) {

        // prevents the event from bubbling up the DOM tree
        // eg the modal from cancelling the event
        e.stopImmediatePropagation();

        var checked = (e.currentTarget.checked) ? false : true;
        e.currentTarget.checked = (checked) ? false : checked.toString();
    });

    function checkbox() {
        if ($('#checkbox-update').is(':checked')) {
            $('#date-temp-update').attr('disabled', 'disabled');
            $('#date-end-update').attr('disabled', 'disabled');
            $('#checkbox-update').val("yes")
        } else {
            $('#date-temp-update').removeAttr('disabled');
            $('#date-end-update').removeAttr('disabled');
            $('#checkbox-update').val("no")
        }
    }
</script>
@section scripts{
    <script>
        var Popup, dataTable;
        $("#pre-load").show();

        $(document).ready(function () {
            dataTable = $('#complex_header').DataTable({
                "language": {
                    "emptyTable": "Không tìm thấy dữ liệu",
                    "info": "Đang hiện _START_ đến _END_ của _TOTAL_ bản ghi",
                    "infoEmpty": "Đang hiện 0 đến 0 của 0 bản ghi",
                    "paginate": {
                        "first": "Trang đầu",
                        "last": "Trang cuối",
                        "next": "Trang sau",
                        "previous": "Trang trước"
                    }
                },
                "ajax": {
                    "url": "/phong-cdvt/kiem-dinh",
                    "type": "POST",
                    "datatype": "json",
                    "data": {
                        "equipmentId": function () { return $('#equipmentId-search').val() },
                        "equipmentName": function () { return $('#equipmentName-search').val() },
                        "dateStart": function () { return $('#date-start-search').val() },
                        "dateEnd": function () { return $('#date-end-search').val() }
                    },
                    "cache": false
                },
                "columns": [
                    { "data": "equipmentId", "name": "equipmentId" },
                    { "data": "equipment_name", "name": "equipment_name" },
                    { "data": "statusname", "name": "statusname" },
                    { "data": "stringExpectedTime", "name": "inspect_expected_date" },
                    { "data": "stringStartTime", "name": "inspect_start_date" },
                    {
                        "data": "updateAble", "render": function (data) {
                            if (data.startsWith("1"))
                                return "<a href=\"#myupdate\" data-toggle=\"modal\" class=\"open-UpdateModal waves-effect waves-light btn blue darken-1 modal-trigger\" data-inspect_id=\"" + data.substr(1) + "\">Bắt&nbsp;đầu</a>";
                            else
                                return "<a href=\"#myedit\" data-toggle=\"modal\" class=\"open-EditModal waves-effect waves-light btn blue darken-1 modal-trigger\" data-inspect_id=\"" + data.substr(1) + "\">Kết&nbsp;thúc</a>";
                        },
                        "orderable": false,
                        "searchable": false,
                        "width": "150px"
                    }
                ],
                "serverSide": "true",
                "order": [3, "desc"],
                "rowId": "incident_id",
                "lengthChange": false,
                "searching": false,
                "initComplete": function (settings, json) {
                    $("#pre-load").hide();
                },
                "preDrawCallback": function (settings) {
                    $('#pre-load').show();
                },
                "drawCallback": function (settings) {
                    $('#pre-load').hide();
                }
            });
        });


        $(document).ready(function () {
            var dataTable = $('#complex_header').DataTable();

            //ajax update
            $("#myupdateform").submit(function (e) {
                $("#pre-load").show();
                $.ajax({
                    type: "POST",
                    url: "/phong-cdvt/kiem-dinh/update",
                    dataType: "text",
                    success: function (data) {
                        dataTable.ajax.reload();

                        $.notify("Cập nhật thành công", {
                            globalPosition: "top center",
                            className: "success"
                        })

                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                        $('#pre-load').hide();
                        $.notify(XMLHttpRequest.responseText, {
                            globalPosition: "top center",
                            className: "error"
                        });
                    },
                    data: {
                        inspect_id: $('#id-update').val(),
                    },
                    cache: false
                }).done(function () {
                    $("#pre-load").hide();
                    $("#exit-update-button").click();
                })
                return false;
            });

            //ajax edit
            $("#myeditform").submit(function (e) {
                $("#pre-load").show();
                $.ajax({
                    type: "POST",
                    url: "/phong-cdvt/kiem-dinh/edit",
                    dataType: "text",
                    success: function (data) {
                        dataTable.ajax.reload();
                        if (JSON.parse(data).success == true)
                            $.notify(JSON.parse(data).message, {
                                globalPosition: "top center",
                                className: "success"
                            })
                        else
                            $.notify(JSON.parse(data).message, {
                                globalPosition: "top center",
                                className: "error"
                            });
                        $('#exit-edit-button').click();
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                        $('#pre-load').hide();
                        $.notify(XMLHttpRequest.responseText, {
                            globalPosition: "top center",
                            className: "error"
                        });
                    },
                    data: {
                        inspect_id: $('#id-edit').val(),
                        dateTemp: $('#date-temp-edit').val()
                    },
                    cache: false
                }).done(function () {
                    $("#pre-load").hide();
                    $("#exit-update-button").click();
                })
                return false;
            });

            //search
            $("#searchButton1").click(function () {
                $("#pre-load").show();
                dataTable.ajax.reload(function () {
                    $.notify("Tìm kiếm thành công", {
                        globalPosition: "top center",
                        className: "success"
                    })
                    $("#pre-load").hide();
                })
            });
        });

        //get data and push to modal update
        $(document).on("click", ".open-UpdateModal", function () {
            var inspect_id = $(this).data('inspect_id');
            $('#id-update').val(inspect_id);
            $('#myupdate h3').text("Bắt đầu kiểm định cho thiết bị " + $(this).parent().parent().children().eq(0).text())
        });

        //get data and push to modal edit
        $(document).on("click", ".open-EditModal", function () {
            var inspect_id = $(this).data('inspect_id');
            $('#id-edit').val(inspect_id);
            var today = new Date();
            var dd = today.getDate();
            var mm = today.getMonth() + 1; //January is 0!

            var yyyy = today.getFullYear();
            if (dd < 10) {
                dd = '0' + dd;
            }
            if (mm < 10) {
                mm = '0' + mm;
            }
            var today = dd + '/' + mm + '/' + (yyyy + 30);
            $('#myedit .minDate').val(today);
            $('#myedit h3').text("Kết thúc kiểm định cho thiết bị " + $(this).parent().parent().children().eq(0).text())
        });
    </script>
}